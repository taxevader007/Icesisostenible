package model;

public class Visitants extends User {

	// The code `public Visitants(String userName, String password) { super(userName, password); }` is a
	// constructor for the `Visitants` class.
	public Visitants(String userName, String password) {
		super(userName, password);
		
	}

	/**
	 * @param userName
	 * @param password
	 */

}