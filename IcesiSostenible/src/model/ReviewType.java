package model;

// The code snippet is defining an enumeration called `reviewType`. An enumeration is a special type in
// Java that represents a fixed set of constants. In this case, the `reviewType` enumeration has six
// constants: `TEXTREVIEW`, `AUDIOS`, `VIDEO`, `PICS`, `TEXT`, and `RESULTSREPORT`. These constants
// represent different types of reviews that can be associated with something in the model.
public enum ReviewType {
	TEXTREVEW,
	AUDIOS,
	VIDEO,
	PICS,
	TEXT,
	RESULTSREPORT
}