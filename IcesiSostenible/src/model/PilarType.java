package model;

// The code snippet is defining an enumeration called `PilarType` with four possible values:
// `BIODIVERSITY`, `WATER`, `TRASHCOLLECTOR`, and `ENERGY`. This enum can be used to represent
// different types or categories of pillars in a model or system.
public enum PilarType {
	BIODIVERSITY,
	WATER,
	TRASHCOLLECTOR,
	ENERGY
}